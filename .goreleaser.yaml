version: 2

builds:
  - goos:
    - linux
    - darwin

kos:
  - repositories: [ghcr.io/eest/goreleaser-test]
    tags:
      - "{{.Version}}"
      - latest
    bare: true
    platforms:
      - linux/amd64
      - linux/arm64
    sbom: none
